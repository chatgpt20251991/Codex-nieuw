# Zero-install demo

From this directory run:

```bash
python3 -m http.server 8080
```

Then open http://localhost:8080

The demo loads the real 71-point project configuration and applies the V1 February-2027 readiness logic locally.
