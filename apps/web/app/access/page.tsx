'use client';import {useEffect,useState} from 'react';import {API_URL} from '../../lib/api';
export default function RestrictedAccess(){const[data,setData]=useState<any>();const[err,setErr]=useState('');useEffect(()=>{const p=new URLSearchParams(location.hash.replace(/^#/,''));const token=p.get('token')||sessionStorage.getItem('eubp_access_token')||'';if(!token){setErr('No access token found.');return}sessionStorage.setItem('eubp_access_token',token);history.replaceState(null,'',location.pathname);fetch(`${API_URL}/restricted-access/session`,{method:'POST',headers:{'x-passport-access-token':token}}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.message||d.code);return d}).then(setData).catch(e=>setErr(e.message))},[]);return <main className="portal"><div className="portalBrand"><div className="logoMark">EU</div><div><strong>Battery Passport</strong><span>Controlled access</span></div></div>{err&&<section className="portalCard"><h1>Access unavailable</h1><p>{err}</p></section>}{data&&<><section className="portalIntro"><div className="kicker">RESTRICTED PASSPORT VIEW</div><h1>{data.battery?.modelIdentifier}</h1><p>{data.battery?.serial} · grant: {data.access?.tier}</p><div className="notice single">Purpose: {data.access?.purpose}</div></section><section className="portalCard"><h2>Authorised data</h2><div className="tableWrap"><table><thead><tr><th>#</th><th>Data point</th><th>Value</th></tr></thead><tbody>{data.values?.map((v:any)=><tr key={v.fieldId}><td>{v.fieldId}</td><td>{v.name}</td><td>{typeof v.value==='object'?JSON.stringify(v.value):String(v.value??'')}</td></tr>)}</tbody></table></div></section></>}</main>}
