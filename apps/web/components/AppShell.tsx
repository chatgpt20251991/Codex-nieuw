'use client';
import Link from 'next/link';import {usePathname} from 'next/navigation';import {ReactNode} from 'react';
const nav=[['/dashboard','Overview'],['/models','Battery models'],['/suppliers','Suppliers'],['/evidence','Evidence'],['/registry','EU Registry'],['/registry-onboarding','Registry setup'],['/onboarding','Onboarding']];
export default function AppShell({children}:{children:ReactNode}){const path=usePathname();return <div className="app"><aside className="sidebar"><Link className="logo" href="/dashboard"><div className="logoMark">EU</div><div><strong>Battery Passport</strong><span>Compliance OS</span></div></Link><nav>{nav.map(([href,label])=><Link key={href} className={path.startsWith(href)?'nav active':'nav'} href={href}>{label}</Link>)}</nav><div className="sideFoot"><span className="dot amber"/>EU battery Registry adapter gated<div className="tiny">No false “registered” state while the integration gate is closed.</div></div></aside><main className="main"><header className="appbar"><div><span className="eyebrow">EU BATTERY PASSPORT INFRASTRUCTURE</span></div><div className="status"><span className="dot green"/>Rule set EU-BR-2026.08</div></header>{children}</main></div>}
