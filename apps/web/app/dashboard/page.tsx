'use client';
import {useEffect,useState} from 'react';import AppShell from '../../components/AppShell';import DevAuthPanel from '../../components/DevAuthPanel';import MetricCard from '../../components/MetricCard';import {apiFetch} from '../../lib/api';
export default function Dashboard(){const [models,setModels]=useState<any[]>([]);const [suppliers,setSuppliers]=useState<any[]>([]);const [error,setError]=useState('');async function load(){try{setModels(await apiFetch('/battery-models'));setSuppliers(await apiFetch('/suppliers'));setError('')}catch(e:any){setError(e.message)}}useEffect(()=>{load()},[]);const items=models.reduce((a,m)=>a+(m._count?.items||0),0);return <AppShell><section className="pageHead"><div><div className="kicker">CONTROL TOWER</div><h1>Battery passport operations, one truth layer.</h1><p>Supplier evidence, validation, immutable passport versions, controlled access and Registry preparation in one workspace.</p></div><button className="button" onClick={load}>Refresh</button></section><DevAuthPanel/>{error&&<div className="alert">{error} — start a dev session or run onboarding.</div>}<div className="metrics"><MetricCard label="Battery models" value={models.length}/><MetricCard label="Battery items" value={items}/><MetricCard label="Suppliers" value={suppliers.length}/><MetricCard label="Registry" value="GATED" detail="Semantic/API readiness flag remains conservative" tone="warn"/></div><section className="panel"><div className="panelHead"><div><span className="kicker">PORTFOLIO</span><h2>Models requiring attention</h2></div></div>{models.length?<div className="rows">{models.map(m=><div className="row" key={m.id}><div><strong>{m.name||m.modelIdentifier}</strong><span>{m.modelIdentifier} · {m.category}</span></div><div className="rowMeta"><span>{m._count?.items||0} items</span><span className="badge neutral">{m.lifecycleStatus}</span></div></div>)}</div>:<div className="empty">No battery models yet. Run onboarding to create the first one.</div>}</section></AppShell>}
