'use client';import {useEffect,useState} from 'react';import AppShell from '../../components/AppShell';import ComplianceMatrix from '../../components/ComplianceMatrix';import {apiFetch} from '../../lib/api';
export default function Models(){const[models,setModels]=useState<any[]>([]);const[err,setErr]=useState('');useEffect(()=>{apiFetch('/battery-models').then(setModels).catch(e=>setErr(e.message))},[]);return <AppShell><section className="pageHead"><div><div className="kicker">BATTERY DATA MODEL</div><h1>Models, items and legal applicability.</h1><p>Model-level facts flow down to physical battery items; item-level lifecycle values can override where required.</p></div></section>{err&&<div className="alert">{err}</div>}<section className="panel"><div className="panelHead"><div><h2>Your battery models</h2></div></div><div className="rows">{models.map(m=><div className="row" key={m.id}><div><strong>{m.name||m.modelIdentifier}</strong><span>{m.modelIdentifier} · {m.category} · {m.chemistry||'chemistry pending'}</span></div><div className="rowMeta"><span>{m._count?.items||0} physical items</span></div></div>)}{!models.length&&<div className="empty">No models found.</div>}</div></section><ComplianceMatrix/></AppShell>}
