const fs=require('fs');const path=require('path');
let ts;for(const p of ['/opt/nvm/versions/node/v22.16.0/lib/node_modules/typescript/lib/typescript.js','/usr/local/lib/node_modules/typescript/lib/typescript.js','/usr/lib/node_modules/typescript/lib/typescript.js']){if(fs.existsSync(p)){ts=require(p);break}}if(!ts){console.error('Global TypeScript module not found');process.exit(2)}
const root=path.resolve(__dirname,'..');const files=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(e.name==='node_modules'||e.name==='dist'||e.name==='.git')continue;const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(/\.tsx?$/.test(e.name))files.push(p)}}walk(root);let failures=0;for(const f of files){const text=fs.readFileSync(f,'utf8');const sf=ts.createSourceFile(f,text,ts.ScriptTarget.Latest,true,f.endsWith('.tsx')?ts.ScriptKind.TSX:ts.ScriptKind.TS);if(sf.parseDiagnostics.length){failures++;console.error('\n'+path.relative(root,f));for(const d of sf.parseDiagnostics)console.error(ts.flattenDiagnosticMessageText(d.messageText,' '))}}console.log(`${files.length} TypeScript/TSX files parsed; ${failures} files with syntax diagnostics.`);process.exit(failures?1:0);
